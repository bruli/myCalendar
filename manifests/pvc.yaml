apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: mycalendar-data
  namespace: mycalendar
spec:
  storageClassName: longhorn
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 50Mi